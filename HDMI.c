//조건 1. 인풋에 연결한 순서 기억 
//조건 2. 인풋 연결 해제시 마지막으로 연결한 인풋을 아웃풋으로 설정
//조건 3. 기본 아웃풋값은 1번 인풋

#include <stdio.h>

int main(){
    char str;
    scanf("%d", &str);
    printf("input : %d", str);

    return 0;
}